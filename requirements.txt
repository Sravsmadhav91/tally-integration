# Core Python deps for scripts/ and sample/ — enough for import, reconcile, review sheets.
# Edge-case OCR/PDF tools live in requirements-optional.txt (install only when needed).
requests>=2.28     # HTTP calls to the Tally gateway
pandas>=1.5        # building/reading review sheets (gap-check, reconciliation)
openpyxl>=3.1      # .xlsx read/write
